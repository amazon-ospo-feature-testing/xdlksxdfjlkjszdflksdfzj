name: do bad things
on:
    workflow_dispatch:

jobs:
    update:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@latest
            - name: bad things
              run: ./commit-test.sh
